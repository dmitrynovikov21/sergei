# MISSION 04: USER CREDITS & SPENDING LIMITS

## 🎯 ЦЕЛЬ
Внедрить систему балансов и лимитов пользователей. Пока что токены считаются, но тратятся бесконечно. Нужно ввести механизм "кредитов":
1. У каждого пользователя есть баланс (в $).
2. Перед выполнением AI-задачи проверяем, хватит ли средств.
3. После выполнения списываем реальную стоимость.

## 🛠 ИНСТРУМЕНТЫ
- **Prisma**: Добавить поле `credits` в модель `User`.
- **Transaction Pattern**: Использовать Prisma `$transaction` для атомарности (списание + запись лога).

## 📋 ПЛАН ДЕЙСТВИЙ

### ЭТАП 1: Schema Update
1. Добавь в модель `User` поле `credits` (тип `Float`, по умолчанию 10.00).
2. Сделай миграцию.

### ЭТАП 2: Credit Manager Service
1. Создай сервис `lib/services/credit-manager.ts`.
2. Реализуй функции:
   - `getUserBalance(userId)` — получить текущий баланс.
   - `checkSufficientFunds(userId, estimatedCost)` — проверка хватит ли денег.
   - `deductCredits(userId, amount, context)` — списание средств.
   - `addCredits(userId, amount, source)` — пополнение (для админа или платежей).

### ЭТАП 3: Интеграция с AI Gateway
1. Вернись в `lib/services/ai-gateway.ts`.
2. **Перед вызовом LLM**:
   - Вызови `checkSufficientFunds(userId, estimatedMaxCost)`.
   - Если средств нет → брось ошибку `InsufficientCredits`.
3. **После вызова LLM** (когда известна реальная цена):
   - Вызови `deductCredits(userId, realCost, context)`.

### ЭТАП 4: UI для баланса
1. Добавь компонент `<CreditBalance />` в Dashboard Header.
2. Должен показывать текущий баланс пользователя и кнопку "Top Up" (пока мок).

## 🧠 БИЗНЕС-ЛОГИКА (SOLID)
- **Атомарность**: Списание и запись лога должны быть в одной транзакции.
- **Fail-Safe**: Если списание не прошло, AI-запрос не должен был выполниться (или откатиться).
- **Audit Trail**: Все пополнения/списания попадают в отдельную таблицу `CreditTransaction`.

## 🚦 DEFINITION OF DONE
1. В базе есть поле `User.credits`.
2. При попытке запустить AI-задачу с нулевым балансом выдается ошибка.
3. После успешного AI-вызова баланс уменьшается на реальную стоимость.
4. В UI видно текущий баланс пользователя.
