# 🔴 Redis Setup Guide

Для работы очередей (BullMQ) требуется работающий инстанс Redis.

## Вариант 1: Docker (Рекомендуемо)
Если у вас установлен Docker, запустите команду:
```bash
docker run -d --name expert-redis -p 6379:6379 redis:alpine
```

## Вариант 2: Локальная установка (Mac)
```bash
brew install redis
brew services start redis
```

## Вариант 3: Облачный Redis (Upstash / Railway)
1. Создайте базу на [Upstash](https://upstash.com/).
2. Получите URL подключения (например, `redis://default:xxxx@...:6379`).
3. Добавьте в `.env`:
   ```
   REDIS_URL=ваша_строка_подключения
   ```

## Проверка
Чтобы проверить, что Redis работает, выполните в терминале:
```bash
redis-cli ping
```
Должен вернуть `PONG`.
