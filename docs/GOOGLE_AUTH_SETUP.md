# 🔐 Настройка Google Auth для проекта

Для того чтобы заработал вход через Google (и регистрация), необходимо создать проект в Google Cloud Console и получить ключи API.

## Шаг 1: Создание проекта
1. Перейдите в [Google Cloud Console](https://console.cloud.google.com/).
2. В верхнем левом углу нажмите на выбор проекта и выберите **"New Project"**.
3. Назовите проект (например, `ExpertOS Dev`) и нажмите **Create**.

## Шаг 2: Настройка User Consent Screen
1. В меню слева выберите **"APIs & Services"** -> **"OAuth consent screen"**.
2. User Type: Выберите **External** (доступно всем, у кого есть Google акканут) и нажмите **Create**.
   - *Примечание:* Если выбрать Internal, вход будет доступен только сотрудникам вашей организации (если у вас Google Workspace).
3. **App Information**:
   - App name: `ExpertOS`
   - User support email: ваш email.
4. **Developer contact information**: ваш email.
5. Нажмите **Save and Continue** (остальные поля можно пропустить).
6. **Scopes**: Нажмите **Add or Remove Scopes**.
   - Выберите `userinfo.email` и `userinfo.profile`.
   - Нажмите **Update**, затем **Save and Continue**.
7. **Test Users**: Добавьте свой email, чтобы вы могли тестировать вход до верификации приложения.
   - Нажмите **Add Users**, введите email, **Save and Continue**.

## Шаг 3: Получение ключей (Credentials)
1. В меню слева выберите **"Credentials"**.
2. Нажмите **+ CREATE CREDENTIALS** -> **OAuth client ID**.
3. **Application type**: Выберите **Web application**.
4. **Name**: `ExpertOS Web Client`.
5. **Authorized JavaScript origins**:
   - `http://localhost:3000` (для локальной разработки)
   - `https://your-production-domain.com` (когда будет прод)
6. **Authorized redirect URIs** (ОЧЕНЬ ВАЖНО):
   - `http://localhost:3000/api/auth/callback/google`
   - `https://your-production-domain.com/api/auth/callback/google`
7. Нажмите **Create**.

## Шаг 4: Добавление в .env
Вам покажут окно с **Client ID** и **Client Secret**. Скопируйте их в ваш файл `.env`:

```env
GOOGLE_CLIENT_ID=ваш_client_id_от_google
GOOGLE_CLIENT_SECRET=ваш_client_secret_от_google
```

## Шаг 5: Проверка
1. Перезапустите сервер (`npm run dev`).
2. Зайдите на `http://localhost:3000/login`.
3. Нажмите кнопку входа через Google.
4. Если все настроено верно, вы должны авторизоваться и попасть в Dashboard.

> ⚠️ **Важно**: Никогда не коммитьте `.env` файл с реальными ключами в репозиторий!
