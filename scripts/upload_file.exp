#!/usr/bin/expect -f
set ip [lindex $argv 0]
set password [lindex $argv 1]
set src [lindex $argv 2]
set dest [lindex $argv 3]

set timeout -1

spawn rsync -av $src root@$ip:$dest
expect {
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
